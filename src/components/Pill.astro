<div class="pill"><slot /></div>

<style>
    .pill {
        display: inline-flex;
        align-items: center;
        padding: 0.5rem 1rem;
        gap: 0.5rem;
        color: var(--gray-100);
        border: 2px solid var(--accent-regular);
        background: linear-gradient(
            135deg,
            rgba(var(--accent-regular-rgb), 0.15) 0%,
            rgba(var(--accent-regular-rgb), 0.05) 100%
        );
        border-radius: 999rem;
        font-size: var(--text-sm);
        font-weight: 600;
        line-height: 1.2;
        white-space: nowrap;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
        backdrop-filter: blur(10px);
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .pill::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(
            135deg,
            rgba(255, 255, 255, 0.1) 0%,
            transparent 50%,
            rgba(255, 255, 255, 0.05) 100%
        );
        opacity: 0;
        transition: opacity 0.3s ease;
    }

    .pill:hover {
        transform: translateY(-2px) scale(1.02);
        border-color: var(--accent-light);
        background: linear-gradient(
            135deg,
            rgba(var(--accent-light-rgb), 0.2) 0%,
            rgba(var(--accent-regular-rgb), 0.1) 100%
        );
        box-shadow:
            0 4px 12px rgba(var(--accent-regular-rgb), 0.3),
            0 0 20px rgba(var(--accent-regular-rgb), 0.1);
        color: white;
    }

    .pill:hover::before {
        opacity: 1;
    }

    /* Animation for staggered appearance */
    @keyframes pillSlideIn {
        from {
            opacity: 0;
            transform: translateX(-10px);
        }
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    .pill {
        animation: pillSlideIn 0.3s ease-out;
    }

    /* Special styling for different technology types */
    .pill:has-text("JavaScript"),
    .pill:has-text("TypeScript") {
        border-color: #f7df1e;
        background: linear-gradient(
            135deg,
            rgba(247, 223, 30, 0.15) 0%,
            rgba(247, 223, 30, 0.05) 100%
        );
    }

    .pill:has-text("React"),
    .pill:has-text("Next.js") {
        border-color: #61dafb;
        background: linear-gradient(
            135deg,
            rgba(97, 218, 251, 0.15) 0%,
            rgba(97, 218, 251, 0.05) 100%
        );
    }

    .pill:has-text("Python") {
        border-color: #3776ab;
        background: linear-gradient(
            135deg,
            rgba(55, 118, 171, 0.15) 0%,
            rgba(55, 118, 171, 0.05) 100%
        );
    }

    .pill:has-text("Node.js") {
        border-color: #68a063;
        background: linear-gradient(
            135deg,
            rgba(104, 160, 99, 0.15) 0%,
            rgba(104, 160, 99, 0.05) 100%
        );
    }

    /* Responsive adjustments */
    @media (max-width: 768px) {
        .pill {
            padding: 0.375rem 0.75rem;
            font-size: var(--text-xs);
        }
    }

    @media (max-width: 480px) {
        .pill {
            padding: 0.25rem 0.5rem;
            font-size: var(--text-xs);
            border-width: 1px;
        }
    }
</style>
